version: 0.2
phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean package
      - mvn liquibase:update
      - echo Specifically Rollback using forward changesets for removing the 500 internal server error

  post_build:
    commands:
      - echo Build Completed Succesfully
artifacts:
  files:
    - target/demo-0.0.1-SNAPSHOT.jar
  discard-paths: yes
